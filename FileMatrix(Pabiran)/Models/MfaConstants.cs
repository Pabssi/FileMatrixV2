namespace FileMatrix_Pabiran_.Models
{
    /// <summary>
    /// Central place for Identity two-factor token provider names (avoid magic strings).
    /// </summary>
    public static class MfaConstants
    {
        public const string EmailTokenProvider = "Email";
    }
}
